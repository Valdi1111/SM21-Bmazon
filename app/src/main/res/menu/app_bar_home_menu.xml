<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/home_search"
        android:icon="@drawable/ic_baseline_search_24"
        android:title="@string/app_bar_search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="ifRoom"
        android:contentDescription="@string/app_bar_search" />

    <item
        android:id="@+id/home_filter"
        android:icon="@drawable/ic_baseline_filter_alt_24"
        android:title="@string/app_bar_filter"
        app:showAsAction="ifRoom"
        android:contentDescription="@string/app_bar_filter" />
</menu>